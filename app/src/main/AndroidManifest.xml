<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.alpha.spendtracker">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <queries>
        <!-- Payment / merchant apps whose launcher icon AppIconImage shows when installed.
             From API 30 a package absent from this list is invisible to PackageManager, so the
             icon lookup returns null and the avatar silently falls back to initials. -->
        <package android:name="com.google.android.apps.nbu.paisa.user" />
        <package android:name="com.phonepe.app" />
        <package android:name="net.one97.paytm" />
        <package android:name="in.swiggy.android" />
        <package android:name="com.application.zomato" />
        <package android:name="com.kirana.consumer" />
        <package android:name="com.zepto.consumer" />
        <package android:name="com.grofers.customerapp" />
        <package android:name="com.dreamplug.android.cred" />
        <package android:name="in.amazon.mShop.android.shopping" />
        <package android:name="com.flipkart.android" />
        <package android:name="com.myntra.android" />
        <package android:name="com.ril.ajio" />
        <package android:name="com.csam.icici.bank.imobile" />
        <package android:name="com.sbi.lotusintouch" />
        <package android:name="com.snapwork.hdfc" />
        <package android:name="com.axis.mobile" />
        <package android:name="com.ubercab" />
        <package android:name="com.olacabs.customer" />
        <package android:name="com.rapido.passenger" />
        <package android:name="com.netflix.mediaclient" />
        <package android:name="com.spotify.music" />

        <package android:name="com.google.android.gms" />
        <package android:name="com.android.vending" />
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="com.google.android.gms.auth.api.signin.SERVICE" />
        </intent>
        <intent>
            <action android:name="com.google.android.play.core.install.BIND_INSTALL_SERVICE" />
        </intent>
        <intent>
            <action android:name="com.google.android.gms.credentials.GET_CREDENTIALS" />
        </intent>
    </queries>

    <application
        android:name=".SpendApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Own task + excluded from recents: this is a transient overlay launched from the
             home screen, not a screen the user should be able to swipe back to. -->
        <activity
            android:name=".widget.QuickAddInputActivity"
            android:exported="false"
            android:theme="@style/Theme.App.QuickAdd"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:taskAffinity=".QuickAdd"
            android:windowSoftInputMode="adjustResize" />

        <receiver
            android:name=".widget.QuickAddWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/quick_add_widget_info" />
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>

</manifest>
